/** Initializes a new Network game and starts running it. */
class Main {
    function void main() {
        var Network game;
        let game = Network.new();
        do game.run();
        do game.dispose();
        return;
    }
}
